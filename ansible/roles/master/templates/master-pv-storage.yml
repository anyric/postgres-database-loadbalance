kind: PersistentVolume
apiVersion: v1
metadata:
  name: postgres-master-pv-volume
  namespace: default
  labels:
    type: local
    app: postgres-master
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
