kind: PersistentVolume
apiVersion: v1
metadata:
  name: postgres-master-pv-volume
  labels:
    type: local
    app: postgres-master
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgres-master-pv-claim
  labels:
    app: postgres-master
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi