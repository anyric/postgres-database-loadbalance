kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgres-slave-pv-claim
  namespace: default
  labels:
    app: postgres-slave
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi